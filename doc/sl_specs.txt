pmkcomp data:

ADD_SYSTEM {
	NAME = "MySystem"
	SL_EXT = "shared library extension"
	SL_VERSION = "$SL_MAJOR.$SL_MINOR"
	SL_NAME = "simple shared library name building"
	SL_NAME_VERS = "versioned shared library name building"
}

pmkfile command:

BUILD_SHLIB_NAME(?) {
	NAME = "library name"
	MAJOR = "major version"
	MINOR = "minor version"
	VARIABLE = "storage variable"
}


example:

(pmkcomp.dat)
ADD_SYSTEM {
	NAME = "OpenBSD"
	SL_EXT = ".so"
	SL_VERSION = "$SL_MAJOR.$SL_MINOR"
	SL_NAME = "lib$SL_LIBNAME$SL_EXT"
	SL_NAME_VERS = "$SL_NAME.$SL_VERSION"
}

(pmkfile)
DEFINE {
	LIBPMK_NAME = "pmk"
	LIBPMK_MAJ = "1"
	LIBPMK_MIN = "2"
}
[...]
BUILD_SHLIB_NAME(?) {
	NAME = "$LIBPMK_NAME"
	MAJOR = "$LIBPMK_MAJ"
	MINOR = "$LIBPMK_MIN"
	VARIABLE = "LIBNAME"
	VERSVAR = "LIBNAMEV"
}

(makefile template)
LIBNAME=	@LIBNAME@
LIBNAMEV=	@LIBNAMEV@

(created makefile)
LIBNAME=	libpmk.so
LIBNAMEV=	libpmk.so.1.2

