common :
- tutorial 
- improve parser to parse variables

pmk :
+ update pmk.1
+ update pmkfile.5
~ compatibility with autoconf (progressing)
- set CPPFLAGS
- set LDFLAGS ?
- cache ? (binary location, etc...)
- more C++ support
- global LANG

pmksetup :
~ pmk.conf(5)
- missing checks :
	- endian type (HW_ENDIAN = BIG_ENDIAN, LITTLE_ENDIAN)
	- char unsigned (pmk ?)
	- long double (pmk ?)
	- better check for install ?
	- others ?
	
pmkscan :
- add more entries in pmkscan.dat

