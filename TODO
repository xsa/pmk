common :
- write specs somewhere in a draft

pmk :
+ handle multiple target files
- include reading of pmk.conf
- pmkfile(5)

pmksetup :
- pmk.conf(5)
- replace old pmk.conf by the new one (aka temporary file)
