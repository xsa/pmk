common :
- write specs somewhere in a draft

pmk :
~ finish pmk(1)
~ pmkfile(5)
~ compatibility with autoconf (progressing)
	- srcdir stuff
	- builddir stuff
- add an option to read additional arguments from a file

pmksetup :
~ pmk.conf(5)
