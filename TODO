common :
- write specs somewhere in a draft

pmk :
~ finish pmk(1)
~ pmkfile(5)
~ compatibility with autoconf (progressing)
	- srcdir stuff
	- builddir stuff
- init CFLAGS, LIBS
- fill check variables with empty value

pmksetup :
~ pmk.conf(5)
