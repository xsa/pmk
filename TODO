common :
- write specs somewhere in a draft
- improve parser to parse variables, quotation marks and spaces near operator.

pmk :
~ finish pmk(1)
~ pmkfile(5)
~ compatibility with autoconf (progressing)
- set CPPFLAGS
- add CHECK_TYPE aka pmk_check_type()
- set LDFLAGS ?
- cache ?

pmksetup :
~ pmk.conf(5)
- missing checks :
	- endian type (HW_ENDIAN = BIG_ENDIAN, LITTLE_ENDIAN)
	- char unsigned (pmk ?)
	- long double (pmk ?)
	- "ln -s"
	- better check for install ?
	- others ?
	
pmkscan :
+ make parsing recursive
- parse c++ files
