common :
- tutorial 
+ improve parser to parse variables
+ force types in parser
! missing function comments

pmk :
+ update pmk.1
? update pmkfile.5 (VARNAME in CHECK_BINARY)
~ compatibility with autoconf (progressing) :
	- support for DEFS (-D options to pass to the compiler)
	- support for LDFLAGS (misc linker options)
	- support for CPPFLAGS (misc linker options)
	- VPATH ?
! cache ? (hash table with binary location, ...)
- more C++ support
! finish global LANG
~ replace po_get_data
- CHECK_VARIABLE ? (cinepaint)
- verify in CHECK_BINARY that program has executable right ?

pmksetup :
! pmk.conf(5) (new format with object)
- missing checks :
	+ endian type (HW_ENDIAN = BIG_ENDIAN, LITTLE_ENDIAN)
	- char unsigned (pmk ?)
	- long double (pmk ?)
	- better check for install ?
	- others ?
+ use main parser
! use objects in pmk.conf
	
pmkscan :
- add more entries in pmkscan.dat

