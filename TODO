			 _______________________
			/			\
			|	TODO list	|
			\_______________________/


#######################################

Format:
Who	Status	Description

Status flags:
	- to do
	+ done
	~ in progress
	! high priority
	. low priority

#######################################


common:
-----------------------------------------------------------------------
	-	tutorial
damien	-	improve test_pmkinstall to check more modes


pmk:
-----------------------------------------------------------------------
	~	compatibility with autoconf (progressing) :
			~ support for DEFS (-D options to pass to the compiler)
			- support for LDFLAGS (misc linker options) ?
			- support for CPPFLAGS (misc linker options) ?
			- VPATH ?
	-	cache ? (hash table with binary location, ...)
	-	more C++ support !
	-	verify in CHECK_BINARY that program has executable right ?
	-	convert gdata.labl to pmk objects ?
	-	checks hash_add() in autoconf.c (void func => bool func)


pmksetup:
-----------------------------------------------------------------------
	!	clean temporary files on error
	-	missing checks :
xavier			+ ECHO_N, ECHO_C, ECHO_T in a specific proc for autoconf variables.
		        . check install binary and use pmkinstall instead if not compatible.
			- char unsigned (pmk ?)
			- long double (pmk ?)
			- better check for install ?
			- others ?


pmkscan
-----------------------------------------------------------------------
	-	add more entries in pmkscan.dat
	-	improve engine to detect types


pmkinstall
-----------------------------------------------------------------------
damien	~!	full rewrite
		+ copy done
		~ mode option
			- suid and sgid bits ?
			+ missing operators (-=)
		+ dir creation
		+ owner
		+ group
		~ strip
dennis		- backup ?


pmkpkg
-----------------------------------------------------------------------
damien	~.	make specs, this tool should handle install and deinstall operations

