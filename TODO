common :
- write specs somewhere in a draft
- improve parser to parse variables
+ pmk objects.

pmk :
~ compatibility with autoconf (progressing)
- set CPPFLAGS
- set LDFLAGS ?
- cache ?

pmksetup :
~ pmk.conf(5)
- missing checks :
	- endian type (HW_ENDIAN = BIG_ENDIAN, LITTLE_ENDIAN)
	- char unsigned (pmk ?)
	- long double (pmk ?)
	- better check for install ?
	- others ?
	
pmkscan :
- add more entries in pmkscan.dat
- specify language of the scanned file.
