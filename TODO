-----------------------------------------------------------------------
Pre Make Kit TODO file

Document revision:
$Id$
-----------------------------------------------------------------------
Format:

Who Status  Description

Status flags:
	-	to do
	+	done
	~	in progress
	!	high priority
	.	low priority
	R	release goal
	S	snapshot goal

Indented lines means subtask.
-----------------------------------------------------------------------


common:
-----------------------------------------------------------------------
	~R	update tutorial
	~.	improve test_pmkinstall to check more modes
	~!	new samples
			- add a sample that show architecture data from pmk
	~R!	improve tag generation and substitution.


pmk:
-----------------------------------------------------------------------
	-R?	check if ~/.pmk.conf is available
	~.	compatibility with autoconf (progressing) :
			~ support for DEFS (-D options to pass to the
				compiler)
			- support for LDFLAGS (misc linker options) ?
			- support for CPPFLAGS (misc linker options) ?
			- VPATH ?
	-.  verify in CHECK_BINARY that program has executable right ?


pmksetup:
-----------------------------------------------------------------------
	~.	cpu identification
			- ppc support (priviledged)
			- sparc support
			- sparc64 support (priviledged)
			~ man page update
	-	cpu optimisation for compiler
	-	option to append to variable of pmk.conf
	+!	clean temporary files on error (verify it's ok)


pmkscan
-----------------------------------------------------------------------
	+S	fix label generation in pmkfile generation
	+S	try opening ./scanfile by default ?
	+S	template configuration header generation :
			+ add CFGNAME option (alternative config
				template name, default config.h.scan)
			+ mark NAME option as obsolete
			+ add MKFNAME option to replace NAME
			+ add ADVTAG option (generate advanced tags,
				default only std autoconf tags ?)
			+ add tags hash table (in structure, init and
				destroy)
			+ add support of tags in check_header() (record
				when found)
			+ add support of tags in check_type() (same as
				above)
			+ update scanfile.5 (CFGNAME,MKFNAME,EXTRATAG)
	-R	add support for advanced tags in pmkscan and pmk
	-R	update scanfile.5 (ADVTAG)
	+S	add support for LIBRARY option in check_header()
	~S!	update pmkscan.1
	-R!	add more entries in pmkscan.dat
	-R	add list of library targets (brainstorming needed)
	-R	add distclean in generated makefiles ?


pmkinstall
-----------------------------------------------------------------------


pmkpc
-----------------------------------------------------------------------
	-.	missing options
			- PMKPC_OPT_UNINST
			- PMKPC_OPT_DEBUG
			- PMKPC_OPT_VAR
			- PMKPC_OPT_VAR_DEF
			- PMKPC_OPT_VAR_PRNT
			- PMKPC_OPT_VAR_SILC
			- PMKPC_OPT_VAR_STDO
	~.	man page
	-.	special expression ('' quoted), post 0.8


misc
-----------------------------------------------------------------------


recursive tasks
-----------------------------------------------------------------------
	~R	track and code missing checks for snprintf and strl*
	~R	update README and INSTALL

